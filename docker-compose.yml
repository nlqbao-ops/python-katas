version: '3.8'

services:
  python-app:
    build:
      context: .
      dockerfile: docker/dockerfile
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    command: tail -f /dev/null